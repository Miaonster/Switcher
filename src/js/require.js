!function(global) {

  var _require = global.require,
      require;

  require = function(module, mode) {
    if (module.indexOf('\.') !== -1) {

    }
  }

  global.require = require();

}(window);
